# My customized version of LazyVim

## Installation (Linux)

```bash
  cd ~/.config
  rm -rf nvim
  git clone https://github.com/sk1ll1ssu3/nvim
```

You can also run this command to remove the git folder and make it your
own repo later

```bash
  cd ~/.config/nvim/
  rm -rf .git
```

## Update

You don't need to run this if this is your own repo now

```bash
  cd ~/.config/nvim/
  git pull
```

## More customization

You can check out <a href="https://www.lazyvim.org/">LazyVim</a>

## My current ----------

I did many failed attempts to install firenvim. I just want not to spend another holiday trying to install it
